@*@model iBoox.Models.ChangeQuestionModel

@{
    ViewBag.Title = "Change Secret Question";
}

<h1 class="title">@ViewBag.Title</h1>

<p>
    Use the form below to change your secret question and answer.
<p/>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true, "Secret question/answer change was unsuccessful. Please correct the errors and try again.")
    <div>
        <fieldset>
            <legend>Account Information</legend>

            <div class="editor-label">
                @Html.LabelFor(m => m.Password)
            </div>
            <div class="editor-field">
                @Html.PasswordFor(m => m.Password, new { value = Model.Password })
                @Html.ValidationMessageFor(m => m.Password)
            </div>

            <div class="editor-label">
                @Html.LabelFor(m => m.SecretQuestion)
            </div>
            <div class="editor-field">
                @Html.TextBoxFor(m => m.SecretQuestion, new { value = Model.SecretQuestion })
                @Html.ValidationMessageFor(m => m.SecretQuestion)
            </div>

            <div class="editor-label">
                @Html.LabelFor(m => m.SecretAnswer)
            </div>
            <div class="editor-field">
                @Html.TextBoxFor(m => m.SecretAnswer)
                @Html.ValidationMessageFor(m => m.SecretAnswer)
            </div>

            <p>
                <input type="submit" value="Change Secret Question" />
            </p>
        </fieldset>
    </div>
}

*@