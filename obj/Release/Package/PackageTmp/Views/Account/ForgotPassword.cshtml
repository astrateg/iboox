@*@model iBoox.Models.RegisterModel

@{
    ViewBag.Title = "Forgot Password";
}

<h1 class="title">@ViewBag.Title</h1>

@using (Html.BeginForm("ForgotPassword", "Account")) {
    if (!String.IsNullOrEmpty(Model.SecretQuestion)) 
    {
        @Html.HiddenFor(m => m.UserName)

        <div class="editor-label">
            @Html.Label(Model.SecretQuestion)
        </div>
        <div class="editor-field">
            @Html.TextBoxFor(m => m.SecretAnswer)
            @Html.ValidationMessageFor(m => m.SecretAnswer)
        </div>
    }
    else
    {
        <div class="editor-label">
            @Html.LabelFor(m => m.UserName)
        </div>
        <div class="editor-field">
            @Html.TextBoxFor(m => m.UserName)
            @Html.ValidationMessageFor(m => m.UserName)
        </div>
    }

    <hr />
    <p><button type="submit" id="user-login-button">Submit</button></p>
}

*@